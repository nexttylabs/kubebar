---
phase: 05-expand-kubectl-data-into-actionable-warning-and-workload-reasons
plan: "03"
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - Kubebar/Views/MenuBarRootView.swift
  - Kubebar/Views/WarningEventsView.swift
  - Kubebar/Views/TrackedItemDetailView.swift
  - docs/architecture/runtime-invariants.md
  - README.md
  - .planning/phases/05-expand-kubectl-data-into-actionable-warning-and-workload-reasons/05-UAT.md
autonomous: true
requirements: [R3, R9, R12]
user_setup: []
must_haves:
  truths:
    - "The menu renders warning event summaries from MenuDisplayModel and caps them at the prepared list."
    - "Tracked item details show state, reason, affected pod count, 1-3 example pod names, and latest related warning when available."
    - "Docs and UAT checks preserve watchlist-first, local-only, no-Secrets, no-raw-output scope."
  artifacts:
    - "Kubebar/Views/WarningEventsView.swift renders count, section notices, and warningEventSummaries."
    - "Kubebar/Views/TrackedItemDetailView.swift renders WatchItemDetailDisplay fields only."
    - "docs/architecture/runtime-invariants.md records warning/detail and partial-failure invariants."
    - ".planning/phases/05-expand-kubectl-data-into-actionable-warning-and-workload-reasons/05-UAT.md provides visible menu checks."
  key_links:
    - "MenuBarRootView passes MenuDisplayModel.warningEventSummaries into WarningEventsView."
    - "WatchlistSectionView continues to pass WatchItemDisplay into TrackedItemDetailView."
    - "Full quality gate confirms SwiftPM and Xcode paths after UI rendering changes."
---

<objective>
Render the new warning summaries and tracked item details in the menu, then document the operator-facing checks for issue #4.

Purpose: The UI should stay watchlist-first and short. It must render prepared display fields from `MenuDisplayModel`, never infer health or expose raw kubectl output.

Output: Updated SwiftUI rendering, concise docs/UAT coverage, and final quality-gate verification.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@README.md
@docs/architecture/system-overview.md
@docs/architecture/runtime-invariants.md
@docs/plans/2026-04-19-002-kubebar-product-roadmap.md
@docs/brainstorms/2026-04-19-kubebar-watchlist-first-requirements.md
@.planning/phases/05-expand-kubectl-data-into-actionable-warning-and-workload-reasons/05-CONTEXT.md
@.planning/phases/05-expand-kubectl-data-into-actionable-warning-and-workload-reasons/05-VALIDATION.md
@.planning/phases/05-expand-kubectl-data-into-actionable-warning-and-workload-reasons/05-PATTERNS.md
@.planning/phases/05-expand-kubectl-data-into-actionable-warning-and-workload-reasons/05-02-SUMMARY.md
@KubebarCore/Models/MenuDisplayModel.swift
@Kubebar/Views/MenuBarRootView.swift
@Kubebar/Views/WarningEventsView.swift
@Kubebar/Views/TrackedItemDetailView.swift
@Kubebar/Views/WatchlistSectionView.swift
@KubebarTests/Models/MenuDisplayModelTests.swift

<interfaces>
Plan 02 must provide these display contracts before this plan runs:

```swift
public struct WarningEventDisplay: Equatable, Sendable, Identifiable {
    public let id: String
    public let reason: String
    public let location: String
    public let age: String
    public let occurrenceCount: Int
    public let message: String?
    public var summary: String { get }
}

public struct WatchItemDetailDisplay: Equatable, Sendable {
    public let stateLabel: String
    public let reason: String
    public let affectedPodCount: Int?
    public let examplePodNames: [String]
    public let latestWarning: WarningEventDisplay?
}

public struct MenuDisplayModel: Equatable, Sendable {
    public let warningEventSummaries: [WarningEventDisplay]
    public let sectionNotices: [SectionAvailabilityDisplay]
}
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MenuDisplayModel -> SwiftUI | Prepared display fields become visible menu text. |
| Documentation/UAT -> future changes | Product boundaries are recorded for later checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Information Disclosure | WarningEventsView and TrackedItemDetailView | mitigate | Views render only `WarningEventDisplay.summary`, short `message`, `WatchItemDetailDisplay`, and section notices. They must not receive raw pod/event records, stdout, stderr, or JSON. Verify by imports and grep. |
| T-05-03-02 | Tampering | UI partial-failure rendering | mitigate | Section notices render visibly near warning events/counters so unavailable data is not hidden. Verify `WarningEventsView` renders `sectionNotices` text and model tests pass. |
| T-05-03-03 | Information Disclosure | Kubernetes read scope documentation | mitigate | Update docs to state the phase reads no Secrets and shows no raw kubectl output. Verify docs mention `Secrets` and `raw kubectl output` in the invariant context. |
| T-05-03-04 | Scope Control | SwiftUI menu | mitigate | Do not add `Open in k9s`, dashboards, raw timelines, scrolling console output, packaging, notarization, or distribution work. Verify grep against touched files. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Render warning summaries and section notices</name>
  <files>Kubebar/Views/MenuBarRootView.swift, Kubebar/Views/WarningEventsView.swift</files>
  <read_first>
    AGENTS.md
    docs/architecture/system-overview.md
    docs/architecture/runtime-invariants.md
    .planning/phases/05-expand-kubectl-data-into-actionable-warning-and-workload-reasons/05-CONTEXT.md
    .planning/phases/05-expand-kubectl-data-into-actionable-warning-and-workload-reasons/05-PATTERNS.md
    KubebarCore/Models/MenuDisplayModel.swift
    Kubebar/Views/MenuBarRootView.swift
    Kubebar/Views/WarningEventsView.swift
  </read_first>
  <action>
Update warning rendering to consume prepared display fields.

In `MenuBarRootView.swift`:
- Replace `WarningEventsView(count: display.counters.warningEvents)` with a call that passes `count: display.counters.warningEvents`, `summaries: display.warningEventSummaries`, and `sectionNotices: display.sectionNotices`.
- Keep the root view as wiring only; do not add event grouping, age calculation, or severity logic.

In `WarningEventsView.swift`:
- Add `import KubebarCore`.
- Update the view signature to `let count: String`, `let summaries: [WarningEventDisplay]`, and `let sectionNotices: [SectionAvailabilityDisplay]`.
- Keep header text exactly `Warning events`.
- If `sectionNotices` is not empty, render each notice as `"{title} unavailable: {reason}"` using `.font(.caption)` and `.foregroundStyle(.secondary)`.
- If `summaries` is empty and `count == "0"`, render exact text `No current warning events`.
- If `summaries` is empty and `count != "0"`, render exact text `"{count} warning events need review"`.
- If `summaries` is not empty, render `ForEach(summaries)` with `Text(summary.summary).lineLimit(1)` and optional `Text(message).lineLimit(2)` when `message` is present.
- Do not render more rows than `summaries` provides; the cap belongs to `HealthEvaluator`.
  </action>
  <verify>
    <automated>swift test --filter MenuDisplayModelTests</automated>
    <automated>swift build</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "WarningEventsView\\(count: display.counters.warningEvents, summaries: display.warningEventSummaries, sectionNotices: display.sectionNotices\\)" Kubebar/Views/MenuBarRootView.swift` finds the root wiring.
    - `rg -n "WarningEventDisplay|SectionAvailabilityDisplay|No current warning events|warning events need review|unavailable:" Kubebar/Views/WarningEventsView.swift` finds the new rendering contract and exact strings.
    - `! rg -n "ClusterSnapshot|WarningEventRecord|kubectl|stdout|stderr|JSONDecoder" Kubebar/Views/WarningEventsView.swift Kubebar/Views/MenuBarRootView.swift`
    - `swift test --filter MenuDisplayModelTests` passes.
    - `swift build` passes.
  </acceptance_criteria>
  <done>The warning section shows compact count fallback, visible section failures, and capped prepared summaries without parsing cluster data in SwiftUI.</done>
</task>

<task type="auto">
  <name>Task 2: Render tracked item details without raw output</name>
  <files>Kubebar/Views/TrackedItemDetailView.swift</files>
  <read_first>
    AGENTS.md
    docs/architecture/system-overview.md
    docs/architecture/runtime-invariants.md
    .planning/phases/05-expand-kubectl-data-into-actionable-warning-and-workload-reasons/05-CONTEXT.md
    .planning/phases/05-expand-kubectl-data-into-actionable-warning-and-workload-reasons/05-PATTERNS.md
    KubebarCore/Models/MenuDisplayModel.swift
    Kubebar/Views/WatchlistSectionView.swift
    Kubebar/Views/TrackedItemDetailView.swift
  </read_first>
  <action>
Update `TrackedItemDetailView` to render `WatchItemDisplay.detail`.

Required visible fields:
- `Text("State: \(item.detail.stateLabel)")`
- `Text(item.detail.reason)`
- If `affectedPodCount` exists, `Text("Affected pods: \(affectedPodCount)")`
- If `examplePodNames` is not empty, `Text("Examples: \(examplePodNames.joined(separator: ", "))")`
- If `latestWarning` exists, `Text("Latest warning: \(latestWarning.summary)")`
- If `latestWarning.message` exists, render it on the next caption line with `lineLimit(2)`.

Keep the detail view confirmatory:
- Use `.font(.caption)`, `.foregroundStyle(.secondary)`, and `.padding(.leading, 6)` like the existing view.
- Do not add buttons, shell handoff, raw pod/event payloads, timelines, or unlimited lists.
- Do not import or reference `ClusterSnapshot`, `WarningEventRecord`, `PodRecord`, `KubectlClusterReader`, or `JSONDecoder`.
  </action>
  <verify>
    <automated>swift test --filter MenuDisplayModelTests</automated>
    <automated>swift build</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "item.detail.stateLabel|Affected pods:|Examples:|Latest warning:" Kubebar/Views/TrackedItemDetailView.swift` finds all required detail rows.
    - `rg -n "lineLimit\\(2\\)" Kubebar/Views/TrackedItemDetailView.swift` finds the latest warning message cap.
    - `! rg -n "ClusterSnapshot|WarningEventRecord|PodRecord|KubectlClusterReader|JSONDecoder|Open in k9s|dashboard|timeline" Kubebar/Views/TrackedItemDetailView.swift`
    - `swift test --filter MenuDisplayModelTests` passes.
    - `swift build` passes.
  </acceptance_criteria>
  <done>Tracked item disclosure content confirms the reason with capped context and no raw Kubernetes output.</done>
</task>

<task type="auto">
  <name>Task 3: Update docs, UAT, and run final gate</name>
  <files>docs/architecture/runtime-invariants.md, README.md, .planning/phases/05-expand-kubectl-data-into-actionable-warning-and-workload-reasons/05-UAT.md</files>
  <read_first>
    AGENTS.md
    README.md
    docs/architecture/runtime-invariants.md
    docs/plans/2026-04-19-002-kubebar-product-roadmap.md
    docs/brainstorms/2026-04-19-kubebar-watchlist-first-requirements.md
    .planning/phases/05-expand-kubectl-data-into-actionable-warning-and-workload-reasons/05-CONTEXT.md
    .planning/phases/05-expand-kubectl-data-into-actionable-warning-and-workload-reasons/05-VALIDATION.md
  </read_first>
  <action>
Document the completed issue #4 behavior and add visible checks.

In `docs/architecture/runtime-invariants.md`:
- Add bullets under Data or Watchlist rules stating warning summaries are capped at 3, grouped by reason plus involved object, and show only reason/location/age/count/short message.
- Add bullets stating tracked item details are limited to state, reason, affected pod count, 1-3 example pod names, and latest related warning.
- Add a bullet stating partial section failures must be visible as unavailable and must not make unavailable data look healthy.
- Preserve the existing no-Secrets invariant.

In `README.md`:
- Update Current Status to mention warning event summaries and workload reasons once implemented.
- Keep product direction as a quick status instrument, not a replacement for deeper tools.
- Do not add install, packaging, notarization, or distribution instructions.

Create `.planning/phases/05-expand-kubectl-data-into-actionable-warning-and-workload-reasons/05-UAT.md` with a short checklist:
- Warning counter remains compact.
- Warning section shows at most 3 summaries.
- Duplicate warnings are grouped.
- Workload row reason is one phrase.
- Detail shows state, reason, affected pod count, 1-3 pod examples, and latest warning when available.
- Malformed/partial section data appears unavailable, not healthy.
- No raw pod/event JSON, full kubectl output, `Open in k9s`, dashboard, or Secrets reads are visible.

Run final verification commands in this order:
1. `swift test --filter KubectlClusterReaderTests`
2. `swift test --filter MenuDisplayModelTests`
3. `swift test --filter RefreshCoordinatorTests`
4. `./scripts/swift-quality-gate.sh local`
  </action>
  <verify>
    <automated>swift test --filter KubectlClusterReaderTests</automated>
    <automated>swift test --filter MenuDisplayModelTests</automated>
    <automated>swift test --filter RefreshCoordinatorTests</automated>
    <automated>./scripts/swift-quality-gate.sh local</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "warning summaries|3|reason|affected pod count|example pod names|latest related warning|partial section" docs/architecture/runtime-invariants.md README.md .planning/phases/05-expand-kubectl-data-into-actionable-warning-and-workload-reasons/05-UAT.md` finds the documented behavior.
    - `rg -n "Secrets|raw kubectl output|Open in k9s|dashboard" .planning/phases/05-expand-kubectl-data-into-actionable-warning-and-workload-reasons/05-UAT.md docs/architecture/runtime-invariants.md` finds scope guard checks.
    - `swift test --filter KubectlClusterReaderTests` passes.
    - `swift test --filter MenuDisplayModelTests` passes.
    - `swift test --filter RefreshCoordinatorTests` passes.
    - `./scripts/swift-quality-gate.sh local` passes.
  </acceptance_criteria>
  <done>Issue #4 has UI rendering, docs/UAT coverage, and full local quality-gate verification.</done>
</task>

</tasks>

<verification>
- `swift test --filter KubectlClusterReaderTests` passes.
- `swift test --filter MenuDisplayModelTests` passes.
- `swift test --filter RefreshCoordinatorTests` passes.
- `./scripts/swift-quality-gate.sh local` passes.
- `! rg -n "Open in k9s|dashboard|notarization|distribution|raw pod|raw event|full kubectl" Kubebar/Views README.md docs/architecture/runtime-invariants.md`
</verification>

<success_criteria>
- R3 remains visible through compact counts.
- R9 tracked details confirm problems without becoming a troubleshooting console.
- R12 partial and stale states remain visibly non-current/non-healthy.
- The final phase gate passes after UI and docs changes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-expand-kubectl-data-into-actionable-warning-and-workload-reasons/05-03-SUMMARY.md`.
</output>
